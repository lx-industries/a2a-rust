# Docker image build jobs for CI runners

.docker-in-docker:
  image: docker:27.5.1-dind@sha256:aa3df78ecf320f5fafdce71c659f1629e96e9de0968305fe1de670e0ca9176ce
  services:
    - docker:27.5.1-dind@sha256:aa3df78ecf320f5fafdce71c659f1629e96e9de0968305fe1de670e0ca9176ce
  variables:
    DOCKER_TLS_CERTDIR: "/certs"
  before_script:
    - docker info
  interruptible: true

build-image:x86_64-unknown-linux-gnu:
  extends: .docker-in-docker
  stage: images
  rules:
    - changes:
        - images/rust/x86_64-unknown-linux-gnu/**/*
    - if: '$IMAGE_BUILD_FORCE == "all" || $IMAGE_BUILD_FORCE == "x86_64-unknown-linux-gnu"'
  script:
    - RUST_VERSION=$(grep "^FROM rust:" images/rust/x86_64-unknown-linux-gnu/Dockerfile | sed -r "s/[^:]*:([0-9]+\.[0-9]+\.[0-9]+).*/\1/")
    - IMAGE_TAG="${CI_REGISTRY_IMAGE}/rust:${RUST_VERSION}-x86_64-unknown-linux-gnu"
    - docker login -u gitlab-ci-token -p ${CI_JOB_TOKEN} ${CI_REGISTRY}
    # Docker Hub convention: variant goes in tag, not image name (like python:3.12-slim, node:20-alpine)
    - docker build -t ${IMAGE_TAG} images/rust/x86_64-unknown-linux-gnu
    - |
      if [ "$IMAGE_PUSH_FORCE" = "all" ] || [ "$IMAGE_PUSH_FORCE" = "x86_64-unknown-linux-gnu" ]; then
        docker push ${IMAGE_TAG}
      elif docker manifest inspect ${IMAGE_TAG} > /dev/null 2>&1; then
        echo "Image ${IMAGE_TAG} already exists, skipping push"
      else
        docker push ${IMAGE_TAG}
      fi

build-image:wasm32-wasip2:
  extends: .docker-in-docker
  stage: images
  rules:
    - changes:
        - images/rust/wasm32-wasip2/**/*
    - if: '$IMAGE_BUILD_FORCE == "all" || $IMAGE_BUILD_FORCE == "wasm32-wasip2"'
  script:
    - RUST_VERSION=$(grep "^FROM rust:" images/rust/wasm32-wasip2/Dockerfile | sed -r "s/[^:]*:([0-9]+\.[0-9]+\.[0-9]+).*/\1/")
    - IMAGE_TAG="${CI_REGISTRY_IMAGE}/rust:${RUST_VERSION}-wasm32-wasip2"
    - docker login -u gitlab-ci-token -p ${CI_JOB_TOKEN} ${CI_REGISTRY}
    # Docker Hub convention: variant goes in tag, not image name (like python:3.12-slim, node:20-alpine)
    - docker build -t ${IMAGE_TAG} images/rust/wasm32-wasip2
    - |
      if [ "$IMAGE_PUSH_FORCE" = "all" ] || [ "$IMAGE_PUSH_FORCE" = "wasm32-wasip2" ]; then
        docker push ${IMAGE_TAG}
      elif docker manifest inspect ${IMAGE_TAG} > /dev/null 2>&1; then
        echo "Image ${IMAGE_TAG} already exists, skipping push"
      else
        docker push ${IMAGE_TAG}
      fi
